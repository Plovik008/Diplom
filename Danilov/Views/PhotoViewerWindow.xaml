<Window x:Class="MuseumAccountingSystem.Views.PhotoViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Просмотр фотографий"
        Height="600" Width="900"
        WindowStartupLocation="CenterOwner"
        Background="#1E1E1E"
        Loaded="Window_Loaded">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#6C3483" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="txtTitle" Grid.Column="0" FontSize="18" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                <Button Grid.Column="1" Content="✕" FontSize="16" Background="Transparent" Foreground="White" BorderThickness="0" Cursor="Hand" Click="BtnClose_Click"/>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="200"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#2D2D2D" CornerRadius="5" Margin="0,0,15,0">
                <Image x:Name="imgMain" Stretch="Uniform" Margin="10"/>
            </Border>

            <Border Grid.Column="1" Background="#2D2D2D" CornerRadius="5" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Все фотографии" FontSize="14" FontWeight="SemiBold" Foreground="#F47B20" Margin="0,0,0,10"/>

                    <ListBox x:Name="lstPhotos" Grid.Row="1" Background="Transparent" BorderThickness="0" SelectionChanged="LstPhotos_SelectionChanged">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#3D3D3D" CornerRadius="3" Margin="2" Padding="3">
                                    <Image Source="{Binding}" Height="60" Stretch="Uniform"/>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="#2D2D2D" Padding="15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="btnPrev" Content="◀ Предыдущая" Background="#6C3483" Foreground="White" FontSize="13" FontWeight="SemiBold" Width="130" Height="35" Cursor="Hand" BorderThickness="0" Click="BtnPrev_Click"/>
                <TextBlock x:Name="txtCounter" VerticalAlignment="Center" Margin="20,0" Foreground="White" FontSize="14"/>
                <Button x:Name="btnNext" Content="Следующая ▶" Background="#6C3483" Foreground="White" FontSize="13" FontWeight="SemiBold" Width="130" Height="35" Cursor="Hand" BorderThickness="0" Click="BtnNext_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>