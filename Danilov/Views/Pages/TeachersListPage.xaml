<Page x:Class="MuseumAccountingSystem.Views.Pages.TeachersListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Преподаватели"
      Background="#ECF0F1">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Преподаватели" FontSize="22" FontWeight="Bold" Foreground="#1A5276" Margin="0,0,0,20"/>

        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="15" Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="txtSearch" Height="35" TextChanged="TxtSearch_TextChanged"/>

                <Button x:Name="btnRefresh" Grid.Column="1" Content="Обновить" Background="#2980B9" Foreground="White" Width="80" Height="35" Margin="10,0,0,0" Click="BtnRefresh_Click"/>
            </Grid>
        </Border>

        <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="15" Margin="0,0,0,20">
            <DataGrid x:Name="dgvTeachers" 
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      SelectionUnit="FullRow">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="ФИО" Binding="{Binding FullName}" Width="250"/>
                    <DataGridTextColumn Header="Кафедра" Binding="{Binding Department}" Width="200"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>
                    <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" Width="150"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Border Grid.Row="3" Background="White" CornerRadius="8" Padding="15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="btnAdd" Content="Добавить" Background="#27AE60" Foreground="White" FontWeight="Bold" Width="120" Height="35" Click="BtnAdd_Click"/>
                <Button x:Name="btnEdit" Content="Редактировать" Background="#2980B9" Foreground="White" FontWeight="Bold" Width="120" Height="35" Margin="10,0,0,0" Click="BtnEdit_Click"/>
                <Button x:Name="btnDelete" Content="Удалить" Background="#E74C3C" Foreground="White" FontWeight="Bold" Width="120" Height="35" Margin="10,0,0,0" Click="BtnDelete_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>