<Page x:Class="MuseumAccountingSystem.Views.Pages.CalendarPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Календарь возвратов"
      Background="#1E1E1E">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Календарь возвратов экспонатов" FontSize="24" FontWeight="Bold" Foreground="#F47B20" Margin="0,0,0,20"/>

        <Border Grid.Row="1" Background="#3D3D3D" Padding="15" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="btnPrevMonth" Content="◀ Предыдущий" Background="#6C3483" Foreground="White" FontSize="13" FontWeight="SemiBold" Width="120" Height="35" Cursor="Hand" BorderThickness="0" Margin="5" Click="BtnPrevMonth_Click"/>
                <TextBlock x:Name="txtCurrentMonth" FontSize="18" FontWeight="Bold" Foreground="White" Width="200" TextAlignment="Center" VerticalAlignment="Center"/>
                <Button x:Name="btnNextMonth" Content="Следующий ▶" Background="#6C3483" Foreground="White" FontSize="13" FontWeight="SemiBold" Width="120" Height="35" Cursor="Hand" BorderThickness="0" Margin="5" Click="BtnNextMonth_Click"/>
                <Button x:Name="btnToday" Content="Сегодня" Background="#F47B20" Foreground="White" FontSize="13" FontWeight="SemiBold" Width="100" Height="35" Cursor="Hand" BorderThickness="0" Margin="5" Click="BtnToday_Click"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Background="#3D3D3D" Padding="15">
            <ItemsControl x:Name="calendarItems" HorizontalAlignment="Stretch">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="7" Rows="6"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#555555" BorderThickness="0.5" Margin="2" Background="{Binding Background}" CornerRadius="5">
                            <StackPanel Margin="5">
                                <TextBlock Text="{Binding Day}" FontSize="16" FontWeight="Bold" Foreground="{Binding DayColor}" HorizontalAlignment="Center"/>
                                <ItemsControl ItemsSource="{Binding Events}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="#F47B20" CornerRadius="3" Margin="0,2,0,0" Padding="3" Cursor="Hand" MouseLeftButtonDown="Event_Click">
                                                <TextBlock Text="{Binding ExhibitName}" FontSize="10" Foreground="White" TextWrapping="Wrap" HorizontalAlignment="Center"/>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>
    </Grid>
</Page>