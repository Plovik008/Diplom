<Window x:Class="MuseumAccountingSystem.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Авторизация" Height="450" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Border Background="White" CornerRadius="15">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#1A5276" CornerRadius="15,15,0,0" Height="120">
                <Grid>
                    <StackPanel VerticalAlignment="Center" Margin="30,0">
                        <TextBlock Text="🏛️" FontSize="40"/>
                        <TextBlock Text="Музей вуза" FontSize="24" FontWeight="Bold" Foreground="White" Margin="0,5,0,0"/>
                        <TextBlock Text="Система учета экспонатов" FontSize="12" Foreground="#BDC3C7"/>
                    </StackPanel>

                    <Button x:Name="btnClose" Content="✕" Width="30" Height="30"
                            HorizontalAlignment="Right" VerticalAlignment="Top"
                            Margin="0,10,15,0" Background="Transparent"
                            Foreground="White" BorderThickness="0" FontSize="16"
                            Cursor="Hand" Click="BtnClose_Click"/>
                </Grid>
            </Border>

            <StackPanel Grid.Row="1" Margin="40,30,40,30">
                <TextBlock Text="Вход в систему" FontSize="18" FontWeight="Bold" 
                           Foreground="#2C3E50" Margin="0,0,0,20"/>

                <TextBlock Text="Логин" FontSize="12" Foreground="Gray" Margin="0,0,0,5"/>
                <TextBox x:Name="txtUsername" Height="40" FontSize="14" Text="admin"/>

                <TextBlock Text="Пароль" FontSize="12" Foreground="Gray" Margin="0,15,0,5"/>
                <TextBox x:Name="txtPassword" Height="40" FontSize="14" Text="admin123"/>

                <Button x:Name="btnLogin" Content="ВОЙТИ" Background="#2980B9"
                        Foreground="White" FontSize="14" FontWeight="Bold"
                        Height="45" Width="200" Margin="0,25,0,0"
                        Cursor="Hand" Click="BtnLogin_Click"/>

                <TextBlock x:Name="txtError" Foreground="#E74C3C" HorizontalAlignment="Center"
                           Margin="0,15,0,0" FontSize="12" Visibility="Collapsed"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>